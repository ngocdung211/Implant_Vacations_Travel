name: Deploy Flask app

on:
  push:
    branches:
      - copilot/setup-flask-backend
  workflow_dispatch:

concurrency:
  group: deploy-medical-clinic
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy on EC2
        run: |
          cp /home/ec2-user/Implant_Vacations_Travel/deploy_backend.sh /tmp/deploy_medical_clinic.sh
          bash /tmp/deploy_medical_clinic.sh
